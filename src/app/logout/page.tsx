import { ComingSoon } from "@/components/coming-soon";

export default function LogoutPage() {
  return <ComingSoon title="Log out" description="Auth isn't wired up yet — there's nothing to log out of." />;
}
